Option to archive or import charts.
Remember chart and/or log data (i.e. reloading and picking up where we left off on startup).
Hard-code system metrics so they can't be changed by user.
Make separate lists that bind to grids in places where this isn't happening yet (like stats, apps).
Combine stats with metrics somehow.
Decouple everything, in particular from UI.
Write automated unit tests.
Replace legacy chart component with a more flexible and modern control like LiveCharts.
Chart customization (colors, series locations, etc.).
Allow user to add new metrics and stats.
Internationalize.
Remember UI positioning and sizing.
Provide daily reports, like kWh used on each day, and average values per day.
Provide basic metrics on MineControl itself, like CPU or memory use.